package com.example.ea.prolink;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.support.v7.widget.LinearLayoutManager;
import android.support.v7.widget.RecyclerView;

import java.util.ArrayList;
import java.util.List;

public class InstructorAppointmentActivity extends AppCompatActivity {
/*    private List<Appointment> appointmentList;
    RecyclerView recyclerView;
    RecyclerViewAdapterAppointment mAdapter;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_instructor_appointment);
        recyclerView = (RecyclerView) findViewById(R.id.recylcerView);
        recyclerView.setHasFixedSize(true);
        recyclerView.setLayoutManager(new LinearLayoutManager(this));
        appointmentList = new ArrayList<>();

        for(int i=0;i<1;i++){
            Appointment appointment=new Appointment(
                    "Ahmed Mohsen" + (i+1),
                    "23-8-2018",
                    "12:00 PM"
            );
            appointmentList.add(appointment);
        }
        mAdapter= new RecyclerViewAdapterAppointment(this,appointmentList);
        recyclerView.setAdapter(mAdapter);
    }*/
}
