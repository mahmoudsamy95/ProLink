<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ea.prolink">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InstructorAppointmentActivity" />
        <activity android:name=".MaterialActivity" />
        <activity android:name=".AcceptedAppointmentActivity" />
        <activity android:name=".RejectedAppointmentActivity" />
        <activity android:name=".UndefinedAppointmentActivity" />

        <activity android:name=".DrawerActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".CourseActivity" />
        <activity android:name=".UsersActivity" />
        <activity android:name=".ShowAppointmentsActivity" />
        <activity android:name=".GradesActivity" />
        <activity android:name=".AllCoursesActivity" />
        <activity android:name=".QuestionsActivity" />
        <activity android:name=".AssignmentsActivity"></activity>
    </application>

</manifest>